# Array operations

# Create arrays
var numbers = [1, 2, 3, 4, 5]
var mixed = [10, 20, 30]

# Array indexing
var first = numbers[0]      # 1
var last = numbers[-1]      # 5 (negative indexing)

# Array iteration and sum
var sum = 0
for num in numbers {
  sum = sum + num
}

# Result: 15 (1 + 2 + 3 + 4 + 5)
sum
